import React, { useEffect, useState } from 'react';

const CustomCursor = () => {
    // Custom cursor disabled for performance optimization
    // Custom cursors cause repaints on every mouse movement which severely impacts scroll performance
    return null;
};

export default CustomCursor;
